# 设计系统规范 · 影响世界的15个科学家

> **v1.2 · 2026-09-14**　补上最后一块拼图：打印样式里的 `#000` 令牌化为 `--print-ink`，
> `:root` 颜色令牌达 **58 个**，16 份样式表与 136 个页面中**独立逃逸的硬编码颜色归零**
> （余下 6 种仅作为 `var()` 内置兜底值存在，属刻意保留）。v1.1 已把规范与代码真正对齐：
> 全部颜色令牌写入 `assets/css/style.css` 的 `:root`。
> 本文与设计稿《影响世界的15个科学家 · Design System》一一对应。

---

## 0. 落地状态（先看这张表）

| 维度 | 规范 | 代码现状 | 状态 |
|---|---|---|---|
| 颜色 | 57 个语义令牌 | 57 个，已全部写入 `:root` | ✅ 闭合 |
| 字体族 | 2 族（sans / mono） | 一致 | ✅ 一致 |
| 字号 | 10 级刻度 | 与刻度一一对应 | ✅ 一致 |
| 圆角 | 5 档 | `--radius-s / --radius` 两档已令牌化，其余为字面值 | ⚠️ 部分 |
| 阴影 | 3 档 | `--shadow-s/m/l` | ✅ 一致 |
| 间距 | 4px 网格 9 档 | 存量值约 41% 落在网格上 | ⚠️ 存量未归并（见 §3） |

---

## 1. 颜色体系 · 57 令牌

规则：靛蓝 `--brand` 是唯一主色，承载品牌识别与全部交互态；琥珀 `--accent` 只做强调，
**不参与导航、不做主按钮**。功能辅色各自只对应一类语义，禁止跨语义混用。

### 1.1 品牌 Brand

| 令牌 | 色值 | 用途 |
|---|---|---|
| `--brand` | `#3B5BDB` | 主色：链接、主按钮、强调数字 |
| `--brand-dark` | `#2F49AF` | 按下 / 悬停的加深态 |
| `--brand-light` | `#6E8BFF` | 品牌渐变的亮端 |
| `--brand-soft` | `#EDF1FD` | 浅底：标签、提示块、图形区 |
| `--brand-hover` | `#DDE5FC` | 胶囊 / 次级块的悬停底 |
| `--brand-line` | `#C7D2FE` | 描边：次级按钮、浅底块边框 |
| `--brand-wash` | `#EEF2FE` | Hero 与页头渐变的起始淡洗 |
| `--brand-ink` | `#2C3E7A` | 品牌浅底上的正文深字 |
| `--brand-a11` … `--brand-a40` | `rgba(59,91,219,.11/.13/.26/.30/.32/.40)` | 品牌透明层：光晕、聚焦环、按钮投影、遮罩 |

平台标记的渐变固定为 `linear-gradient(140deg, var(--brand), var(--brand-light))`。

### 1.2 强调色 Accent

| 令牌 | 色值 | 用途 |
|---|---|---|
| `--accent` | `#F59F00` | 强调：提示条左标、「先猜一猜」 |
| `--accent-soft` | `#FFF8E9` | 浅底 |
| `--accent-line` | `#F2D675` | 描边 |
| `--accent-wash` | `#FFF6E5` | Hero 渐变的暖端；`--accent-wash-2` `#FFF4E0` 门户专用 |
| `--accent-ink` | `#A16207` | 琥珀标签与类比标题的深字 |
| `--accent-ink-2` | `#B26A00` | 门户变体 |
| `--accent-deep` | `#854F0B` | 「猜一猜」粗体字 |
| `--accent-ink-deep` | `#412402` | 「猜一猜」正文 |

> ⚠️ **待收敛**：琥珀深字现有 4 档（`#A16207` / `#B26A00` / `#854F0B` / `#412402`）。
> 它们目前各自承担不同底色上的对比度需求，属于**有意分层**；若要精简到 2 档，
> 必须逐个复测对比度，不能直接合并。

### 1.3 功能辅色 Support

| 语义 | 主色 | 浅底 | 深字 | 描边 |
|---|---|---|---|---|
| 青绿 · 实验 / 正向确认 | `--green` `#0CA678` | `--green-soft` `#E6FCF5` | `--green-ink` `#087F5B` | `--green-line` `#B2F0DC` |
| 紫 · 延伸阅读 | `--purple` `#7048E8` | `--purple-soft` `#F1EBFF` | `--purple-ink` `#5B30C4` | — |
| 橙 · 警示 / 温度 | `--orange` `#E8590C` | `--orange-soft` `#FFF0E8` | `--orange-ink` `#C2410C` | — |

一律「浅底 + 同色深字」成对使用，不得浅底配黑字、也不得深字配异色底。

### 1.4 中性色 Neutral

| 令牌 | 色值 | 用途 |
|---|---|---|
| `--ink` | `#1B2530` | 标题、页脚底色 |
| `--ink-strong` | `#14202B` | `strong` 强调（比 `--ink` 略深） |
| `--ink-body` | `#2B3644` | 详解页正文专用 |
| `--ink-2` | `#56617A` | 正文、次要信息 |
| `--ink-soft` | `#5C6B82` | 浅底上的合格灰（4.62:1） |
| `--ink-3` | `#8B96AA` | 说明文字（**仅限深底**，浅底见 §1.7） |
| `--line` | `#E3E8EF` | 常规描边 |
| `--line-2` | `#D4DBE5` | 重描边、滑轨 |
| `--bg-alt` | `#EDF1F7` | 次级底色、读数条 |
| `--bg` | `#F7F9FC` | 页面底色 |
| `--bg-elev` | `#FBFCFE` | 悬停浮起底、画布底 |
| `--card` | `#FFFFFF` | 卡片面 |
| `--white` | `#FFFFFF` | 纯白（与 `--card` 同值不同语义） |
| `--img-bg` | `#F4F6FA` | 图片衬底（`object-fit: contain` 留白处） |
| `--swatch-gray` | `#CCCCCC` | 图例占位色块 |

> `--card` 与 `--white` 同值。前者是「卡片表面」这一语义位，后者是「纯白」。
> 新代码优先用语义更准的那个，**不要因为同值就随手互换**。

### 1.5 页脚暗底 Foot（页脚与暗底专用）

| 令牌 | 色值 | 用途 |
|---|---|---|
| `--ink` | `#1B2530` | 页脚底色（复用中性色） |
| `--foot-fg` | `#A9B4C4` | 页脚正文 |
| `--foot-link` | `#CBD5E1` | 页脚链接 |
| `--foot-line` | `#2C3846` | 页脚分隔线 |
| `--foot-dim` | `#7B8798` | 版权行等最次要信息 |

### 1.6 高亮 Highlight

`--highlight-bg` `#FFF3BF` + `--highlight-fg` `#6B4E12`。
仅用于术语释义与公式要点，**每屏不超过 3 处**。

### 1.7 效果层 Effect

| 令牌 | 值 | 用途 |
|---|---|---|
| `--nav-bg` | `rgba(255,255,255,.86)` | 吸顶导航半透明底 |
| `--overlay` | `rgba(20,30,45,.42)` | 弹窗遮罩 |
| `--ink-a18` | `rgba(27,37,48,.18)` | 中性描边透明层 |

### 1.8 对比度底线（硬性要求）

- 正文文字 **≥ 4.5:1**
- 24px 以上标题、图标、描边 **≥ 3:1**
- ⚠️ **已知坑**：旧值 `--ink-3 #8B96AA` 落在浅底上仅 **1.87:1**，已统一替换为
  `--ink-soft #5C6B82`（**4.62:1**）。**图形描边不在此列**——同一色值同时用于文字与描边时
  必须按 (站, 实验, 颜色) 精修，全局替换会改坏画面。
- ⚠️ `labs.html` 的**实验图例色板**（`#E03131` / `#9C36B5` / `#51CF66` / `#FAB005` 等）
  **刻意不纳入全局令牌**：它们是单张画布内的数据系列区分色，按既定设计「每张画布就地配图例」。
  唯一硬约束是 §5 的「一个颜色在一个画布里只能表示一件事」。

---

## 2. 字体层级

两个字体族，**不加载 Web 字体**，回落到各平台系统字体（macOS 苹方 / Windows 微软雅黑）。

```
--sans: -apple-system, BlinkMacSystemFont, "PingFang SC", "Hiragino Sans GB",
        "Microsoft YaHei", "Helvetica Neue", Arial, sans-serif
--mono: ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace
```

等宽只用于公式、读数、年份、坐标刻度与令牌名；**中文正文禁止使用等宽**。

| 层级 | 字号 / 字重 | 行高 | 字距 | 用途 |
|---|---|---|---|---|
| Display | 46 / 800 | 62 | −1.4 | 封面主标题、空状态大标题 |
| H1 | 38 / 800 | 51 | −0.9 | 页面头 `h1`（`page-head`） |
| H2 | 30 / 800 | 41 | −0.6 | 区块标题 `sec-head h2` |
| H3 | 25 / 800 | 36 | −0.4 | 详解页小节 `article h2` |
| H4 | 19 / 700 | 28 | 0 | 卡片标题、时间轴节点 |
| Body L | 17 / 400 | 31 | 0 | 首页正文（`body` 默认） |
| Body | 16.5 / 400 | 30 | 0 | 详解页正文、术语释义 |
| Body S | 15.5 / 400 | 28 | 0 | 弹窗正文、时间轴说明 |
| Caption | 13.5 / 400 | 23 | 0 | 图注、面包屑 |
| Micro | 12.5 / 700 | 20 | +1.2 | 区块标签、分类胶囊、徽章 |
| Mono | 28 / 800（等宽） | 38 | +0.5 | 公式卡、实验读数 |

**规则**：标题固定 800、正文固定 400，不加中间字重；靠字号与留白拉开层级，不靠加粗堆叠。

---

## 3. 间距规则

### 3.1 目标刻度（4px 基数，9 档）

`4 · 8 · 12 · 16 · 20 · 24 · 30 · 40 · 60`

| 令牌 | 值 | 典型用途 |
|---|---|---|
| `--space-1` | 4 | 标签内边距、紧邻元素 |
| `--space-2` | 8 | 图标与文字、标签行 |
| `--space-3` | 12 | 按钮组、紧凑列表 |
| `--space-4` | 16 | 卡片网格 gap、表单控件 |
| `--space-5` | 20 | 卡片内边距、按钮横向 |
| `--space-6` | 24 | 主卡片内边距、区块内 |
| `--space-7` | 30 | 区块标题下间距 |
| `--space-8` | 40 | 区块之间 |
| `--space-9` | 60 | 页面分节 `section` |

### 3.2 存量值：为什么不强行归并

站内实际在用的间距值里，约 **41%** 落在上述九档上，其余是
`7 / 9 / 11 / 13 / 14 / 18 / 22 / 26` 等值。**本轮刻意没有做全局替换**，原因：

1. 偏离值里绝大多数是**组件内边距**，不是布局节奏——例如标签 `padding: 4px 11px`、
   按钮 `padding: 14px 16px`、区块 `padding: 66px 24px 60px`。它们是**组件尺寸的一部分**，
   吸附到网格会真实改变视觉（差 ±1~2px），而收益只是数值整齐。
2. 还有一小部分是**有意的像素补偿**，最典型的是 `margin-right: 2px`
   （图标与文字的光学对齐）。强行改成 4px 会看出错位。
3. 15 个子站的样式表本身**已经有 96.4% 的声明完全一致**（1152 个共有声明里仅 41 处不同，
   且大多源于页面结构差异）。设计系统最核心的价值——一致性——已经达成。

**因此本节的执行规则是分层的：**

| 场景 | 要求 |
|---|---|
| 新增组件 / 重写样式 | **必须**落在九档上 |
| 存量微调（改颜色、改文案） | 不动间距 |
| 成规模的组件重构 | 按**组件逐个**校准，并在子站渲染回归后提交，不做全局正则替换 |

---

### 3.3 圆角刻度

`10 --radius-s`（胶囊 / 小控件） · `14`（卡片） · `16 --radius`（主卡片） ·
`20`（弹窗） · `999`（全圆）

### 3.4 阴影层级

| 令牌 | 值 | 用途 |
|---|---|---|
| `--shadow-s` | `0 1px 2px rgba(27,37,48,.05)` | 静息态 |
| `--shadow-m` | `0 4px 16px rgba(27,37,48,.07)` | 悬停、时间轴展开、吸顶导航 |
| `--shadow-l` | `0 18px 50px rgba(27,37,48,.16)` | 术语弹窗、底部抽屉 |

只允许叠一层，禁止发光色阴影。

### 3.5 栅格与断点

| 项 | 值 |
|---|---|
| 内容最大宽度 | `--max-w: 1160px` |
| 导航高度 | `--nav-h: 62px` |
| 左右内边距 | 桌面 24px · 手机 16px |
| 响应断点 | ≥1000 双栏 · 640–999 单栏两列 · <640 单栏 |
| 卡片网格 gap | 18（密集区 14） |

桌面优先向下收敛：移动端次要面板一律改为**底部抽屉**，不做横向滚动。

---

## 4. 基础组件

### 按钮 Buttons
圆角 11 · 内边距 11/20 · 字重 700。主按钮（`--brand` 底 + 白字 + 主色投影）**每屏至多一个**；
次级用白底 + `--brand-line` 1.5px 描边；小号（圆角 9 / 7-14）；
禁用态 `--bg-alt` 底 + `--ink-3` 字；橙色仅用于不可逆操作。

### 标签 · 胶囊 · 徽章
一律「浅底 + 同色深字」成对出现，**颜色即语义**：

| 类型 | 规格 |
|---|---|
| 分类标签 | 圆角 8 · 内边距 5/12 · 12.5pt Bold |
| 术语胶囊 | 全圆 · 内边距 5/13 · 13pt SemiBold · 悬停加深 |
| 徽章 | 圆角 7 · 内边距 4/10 · 11.5pt ExtraBold |
| 区块标签 | 圆角 7 · 12pt ExtraBold · 字距 +1.2 · 全大写英文 |

### 表单控件
- **搜索框**：圆角 13 · 内边距 15/18 · 1.5px `--line-2` 描边；聚焦转 `--brand` 描边 + 3px 主色光晕。
- **分类筛选**：全圆 · 内边距 9/18 · 14pt Bold；选中为 `--brand` 实底白字，默认白底描边。
- **滑块**：轨道 5px；把手 20px 白底 + 3px `--brand` 环 + 主色投影。
  **拖动必须同步更新读数条**（`--bg-alt` 底 · 等宽 14.5pt · 居中），不得静默。

### 卡片 Cards
静息 `--shadow-s` + `--line` 描边；悬停上移 3px、转 `--shadow-m`、描边转 `--brand-line`。
- **成就卡片**：圆角 16，顶部 132px 图形区（`--brand-soft` 底）+ 年份（等宽 12）+ 标题（19/800）+ 描述（14.5）+ 进入链接。
- **术语卡**：圆角 14，分类胶囊 + 标题（17/800）+ 描述（14.5）+ 「查看释义 →」。

### 提示块 · 内联元素
- **提示块**：圆角 14 · 内边距 18/20，按语义取色（品牌 / 琥珀 / 青绿），标题 14/800 + 正文 15。
  同一篇内不超过三种。
- **术语**：正文中主色 + 点划下划线，点击弹出释义卡。
- **高亮**：`--highlight-bg` 底纹 + `--highlight-fg` 字，只标结论与公式要点。

---

## 5. 使用规范

### Do
- **颜色只从 57 个令牌里取**，不新增任何十六进制或 `rgba()` 字面值。
- 写页面结构用 class，**不要用内联 `style=""`**——内联样式是绕过样式表的主要途径。
  （存量内联样式里的颜色已于 v1.1 收敛为 `var()`，但结构属性仍是内联。）
- 字号只用 10 级刻度；标题固定 800，正文固定 400。
- 新增组件的间距落在 4px 网格上，同一容器只用一个 `gap`。
- 每个交互都必须有反馈：读数变化、描边变色或阴影浮起。

### Don't
- 不要用 `--accent` 做导航或主按钮，它只负责强调。
- **不要让同一个颜色在一个画布里表示两件事**（实验图例的硬约束）。
- 不要在中文正文里混用等宽字体，字形宽度不齐会跳行。
- 不要在浅底上用低于 4.5:1 的灰色写正文。
- 不要因为 `--card` 和 `--white` 同值就随手互换。

### 落地入口
1. **读令牌** — 变量面板：`Color / Space / Radius / Typography` 四个集合；
   或直接看 `assets/css/style.css` 的 `:root`（57 色 + 9 项非颜色）。
2. **对样式** — 逐项对照 `:root`，命名一一对应。
3. **提新组件** — 先在规范页补齐规格与状态，再动子站代码，避免 15 个站各写一套。

---

## 6. 变更记录

### v1.2 · 2026-09-14 —— 逃逸颜色归零
- 打印样式中的 `a { color: #000 }` 令牌化为 `--print-ink`。该值位于 `@media print` 内，
  语义是「打印墨色」而非品牌色——直接换成 `var(--brand)` 会让打印件出现彩色链接，
  故改配等值语义令牌，兼顾语义正确与零视觉变化。工具：`tools/tokenize_print_link.py`。
- `:root` 颜色令牌 57 → **58**；独立逃逸颜色 **0 种**。
- 校验：`tools/verify_tokenize.py` 16/16 通过（展开 `var()` 后与改前逐字节一致）。

### v1.1 · 2026-09-14 —— 令牌体系闭合
- **新增 36 个颜色令牌**（品牌扩展、功能辅色深字、中性扩展、页脚暗底、高亮、效果层），
  在此之前它们以硬编码字面值散落在样式表里。
- **CSS 令牌化 1263 处 / 16 个文件**：逃逸颜色从 38 种降到 1 种（仅保留 `a{color:#000}` 待议）。
- **HTML 内联样式令牌化 491 处 / 136 个文件**：内联样式里的品牌色与中性色改为 `var()`。
- **零视觉变化的证明方式**：把改后文件里的 `var()` 展开回字面值，与改前版本逐字节比对
  （含 3 位 / 6 位十六进制归一），16 份 CSS 与 136 个页面**全部一致**。
  校验脚本：`tools/verify_tokenize.py`、`tools/verify_html_tokenize.py`。
- 回归：`validate_site.py` 106 通过 / 0 警告 / 0 错误；15 子站 + 门户 e2e 通过。

### v1.0 · 2026-09
- 首版规范：颜色 33 令牌、字体 10 级、间距 9 档、圆角 5 档、阴影 3 层、基础组件规格。
- ⚠️ 首版把 33 个令牌写成「提取自站内既有变量」并**不准确**：其中 12 个
  （`--*-ink`、`--inverse-*`、`--highlight-*`、`--ink-body`、`--ink-soft`）当时
  在代码里并不存在，是在 v1.1 才真正补齐并入 `:root` 的。

---

*影响世界的 15 个科学家 · 设计系统 v1.1 · 2026-09-14*
*修改令牌前请跑一遍 15 个子站的渲染回归。*
